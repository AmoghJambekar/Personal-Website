---
const navItems = [
  { id: 'bio' },
  { id: 'projects' },
  { id: 'experience' },
  { id: 'education' },
];
---

<nav class="fixed bottom-6 left-1/2 -translate-x-1/2 flex gap-4 z-50">
  {navItems.map(({ id }) => (
    <a href={`#${id}`} class="w-3 h-3 bg-gray-400 rounded-full hover:scale-125 transition-all duration-200" aria-label={id}></a>
  ))}
</nav>
